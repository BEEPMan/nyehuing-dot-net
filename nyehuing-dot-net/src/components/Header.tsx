import React from 'react';
import { Title, Menu } from '../containers';

const Header: React.FC = () => {
  return (
    <div>
      <Title />
      <Menu />
    </div>
  );
};

export default Header;